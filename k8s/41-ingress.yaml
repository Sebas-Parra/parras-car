# Host de desarrollo local. Agregar a /etc/hosts:
#   <IP de minikube>  yepez-sagnay-parra.local
# (obtené la IP con `minikube ip`)
#
# Dos Ingress separados sobre el mismo host: el de /api lleva
# rewrite-target (necesita 2 grupos de captura) y el de / no debe
# reescribir nada, así que van en objetos distintos en vez de compartir
# una única anotación por-Ingress.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yepez-sagnay-parra-api-ingress
  namespace: yepez-sagnay-parra
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host: yepez-sagnay-parra.local
      http:
        paths:
          - path: /api(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: kong
                port:
                  number: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yepez-sagnay-parra-dashboard-ingress
  namespace: yepez-sagnay-parra
spec:
  ingressClassName: nginx
  rules:
    - host: yepez-sagnay-parra.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dashboard
                port:
                  number: 80
